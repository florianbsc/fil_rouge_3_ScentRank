{
  "dependencies": {
    "cors": "^2.8.5",
    "joi": "^18.0.1",
    "morgan": "^1.10.1"
  },
  "scripts": {
    "dev:up": "docker compose -f infra/docker/docker-compose.dev.yml --profile dev up --build",
    "dev:down": "docker compose -f infra/docker/docker-compose.dev.yml --profile dev down"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/morgan": "^1.9.10"
  }
}
