{
  "dependencies": {
    "joi": "^18.0.1"
  },
"scripts": {
  "dev:up": "docker compose -f docker-compose.dev.yml --profile dev up --build",
  "dev:down": "docker compose -f docker-compose.dev.yml --profile dev down -v"
  }
}
